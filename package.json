{
  "name": "underkeep",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:single": "npm run build && node scripts/singlefile.mjs dist/underkeep.html"
  },
  "dependencies": {
    "@fontsource/unifrakturcook": "^5.3.0",
    "@fontsource/vt323": "^5.3.0",
    "three": "^0.186.1"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.1"
  }
}
